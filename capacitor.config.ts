import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Prototype-tax-invoice',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
